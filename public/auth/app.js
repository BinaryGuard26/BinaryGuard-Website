const screens={tenant:document.querySelector("#tenantScreen"),register:document.querySelector("#registerScreen"),login:document.querySelector("#loginScreen"),verify:document.querySelector("#verifyScreen"),recover:document.querySelector("#recoverScreen"),service:document.querySelector("#serviceScreen")};
const titles={tenant:"User Authentication",register:"Register",login:"Login",verify:"Verify OTP",recover:"Recover Access",service:"Service Authorization"};
const state={registered:false,tenantVerified:false,otpVerified:false,email:"john.smith@gov.mb.ca",domain:"",otp:"248106",otpAttempts:0,maxOtpAttempts:3};
const approvedDomains=["gov.mb.ca","clientabc.com","cityofx.ca"];
const screenTitle=document.querySelector("#screenTitle"),statusPill=document.querySelector("#statusPill"),auditLog=document.querySelector("#auditLog"),flowButtons=document.querySelectorAll(".flow-step[data-flow]"),clearLogBtn=document.querySelector("#clearLogBtn");
const registerBtn=document.querySelector("#registerBtn"),loginBtn=document.querySelector("#loginBtn"),verifyBtn=document.querySelector("#verifyBtn"),recoverBtn=document.querySelector("#recoverBtn"),openServiceBtn=document.querySelector("#openServiceBtn");
function showScreen(name){Object.entries(screens).forEach(([key,screen])=>screen.classList.toggle("active",key===name));flowButtons.forEach(button=>button.classList.toggle("active",button.dataset.flow==="tenant"));screenTitle.textContent=titles[name]}
function log(message){const item=document.createElement("li");const time=new Date().toLocaleTimeString([],{hour:"2-digit",minute:"2-digit"});item.textContent=`${time} - ${message}`;auditLog.prepend(item)}
function toast(message){document.querySelector(".toast")?.remove();const node=document.createElement("div");node.className="toast";node.textContent=message;document.body.append(node);window.setTimeout(()=>node.remove(),2800)}
function updateStatus(text,ok=false){statusPill.textContent=text;statusPill.classList.toggle("ok",ok)}
function getDomain(email){return email.split("@")[1]?.trim().toLowerCase()||""}
function isApprovedDomain(email){return approvedDomains.includes(getDomain(email))}
function validateDomain(email){if(!email.includes("@")){toast("Enter a valid corporate email address.");log("Tenant authentication stopped because email format was invalid.");return false}if(!isApprovedDomain(email)){toast("Your organization is not authorized. Please contact BinaryGuard.");log(`Access denied. Unauthorized domain attempted: ${email}`);return false}state.email=email;state.domain=getDomain(email);state.tenantVerified=true;toast("Domain approved. OTP generated.");log(`Domain approved through allowed_domains: ${state.domain}.`);log("OTP generated and sent to approved corporate email.");updateStatus("OTP Issued");return true}
function otpCode(){return[...document.querySelectorAll("#codeInputs input")].map(input=>input.value).join("")}
document.querySelectorAll("[data-flow-target]").forEach(button=>button.addEventListener("click",()=>showScreen(button.dataset.flowTarget)));
flowButtons.forEach(button=>button.addEventListener("click",()=>showScreen("tenant")));
document.querySelectorAll("#codeInputs input").forEach((input,index,list)=>input.addEventListener("input",()=>{input.value=input.value.replace(/\D/g,"").slice(0,1);if(input.value&&list[index+1])list[index+1].focus()}));
registerBtn.addEventListener("click",()=>{const name=document.querySelector("#regName").value.trim(),email=document.querySelector("#regEmail").value.trim().toLowerCase(),organization=document.querySelector("#regOrg").value.trim();if(!name||!organization){toast("Enter full name and organization.");log("Registration stopped because required identity details were missing.");return}if(!validateDomain(email))return;state.registered=true;document.querySelector("#loginEmail").value=email;document.querySelector("#recoverEmail").value=email;log(`User record prepared for ${name} at ${organization}.`);showScreen("verify")});
loginBtn.addEventListener("click",()=>{const email=document.querySelector("#loginEmail").value.trim().toLowerCase();if(!validateDomain(email))return;log("Tenant status checked: active.");log("User status checked: active.");showScreen("verify")});
verifyBtn.addEventListener("click",()=>{if(!state.tenantVerified){toast("Please register or login first.");showScreen("tenant");return}state.otpAttempts+=1;if(state.otpAttempts>state.maxOtpAttempts){toast("OTP attempt limit exceeded.");log("OTP verification blocked after maximum attempts.");return}if(otpCode()!==state.otp){toast("Invalid verification code.");log(`OTP failed. Attempt ${state.otpAttempts} of ${state.maxOtpAttempts}.`);return}state.otpVerified=true;updateStatus("Verified",true);toast("OTP verified. Session created.");log("OTP valid. Session created with token_hash and expiry.");log("OTP marked used and destroyed after successful verification.");showScreen("service")});
recoverBtn.addEventListener("click",()=>{const email=document.querySelector("#recoverEmail").value.trim().toLowerCase();if(!validateDomain(email))return;toast("Recovery code sent.");log(`Recovery process started for ${email}.`);log("Recovery OTP generated and sent to approved corporate email.");showScreen("verify")});
openServiceBtn.addEventListener("click",()=>{toast("Opening Access Card Ordering Portal.");log("User continued to Access Card Ordering Portal.")});
clearLogBtn.addEventListener("click",()=>auditLog.innerHTML="");
updateStatus("Awaiting Verification");log("User Authentication page loaded.");log("Register, Login, Verify OTP, and Recover Access are ready.");
